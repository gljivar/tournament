<h1><span ng-show="fight.competitor_blue_id != null" class="text-info" ng-class="{'icon-trophy':fight.competitor_winner_id == fight.competitor_blue_id}">{{fight.competitor_blue.last_name}}</span>
<span ng-show="fight.competitor_blue_id == null" class="text-info">Winner of {{fight.previous_fight_blue.field_initial.name}}-{{fight.previous_fight_blue.number}}</span>
- 
<span ng-show="fight.competitor_red_id != null" class="text-error" ng-class="{'icon-trophy':fight.competitor_winner_id == fight.competitor_red_id}">{{fight.competitor_red.last_name}}</span>
<span ng-show="fight.competitor_red_id == null" class="text-error">Winner of {{fight.previous_fight_red.field_initial.name}}-{{fight.previous_fight_red.number}}</span>
</h1>

<p> 
  <b>Number initial</b>
  <div class="badge badge-info">{{fight.field_initial.name}}-{{fight.number}}</div>
  <span ng-show="fight.field_actual.name != fight.field_initial.name || fight.number_actual!= fight.number">
  <span class="icon-double-angle-right"></span> 
  <div class="badge badge-info">{{fight.field_actual.name}}-{{fight.number_actual}}</div>
  </span>
</p>

<p>
  <b>Category</b><br>
  {{fight.category.name}}
</p>

<p>
  <b>Competitor blue</b><br>
  {{fight.competitor_blue.last_name}}
  <span ng-show="fight.competitor_blue_id == null">Winner of {{fight.previous_fight_blue.field_initial.name}}-{{fight.previous_fight_blue.number}}</span>
</p>

<p>
  <b>Competitor red</b><br>
  {{fight.competitor_red.last_name}}
  <span ng-show="fight.competitor_red_id == null">Winner of {{fight.previous_fight_red.field_initial.name}}-{{fight.previous_fight_red.number}}</span>
</p>

<p>
  <b>Competitor winner</b><br>
  {{fight.competitor_winner.last_name}}
  <span ng-show="fight.competitor_winner_id == null">Fight not yet finished</span>
</p>

<p>
  <b>Previous fight blue</b><br>
  <div ng-show="fight.previous_fight_blue.id != null">
  <a ng-show="fight.previous_fight_blue.id != null"  class="a-white" href="/fights/{{fight.previous_fight_blue.id}}">
    <div class="badge badge-info">{{fight.previous_fight_blue.field_initial.name}}-{{fight.previous_fight_blue.number}}</div>
  </a>
  </div>
  <span ng-show="fight.previous_fight_blue_id == null">No previous blue fight</span> 
</p>

<p>
  <b>Previous fight red</b><br>
  <div ng-show="fight.previous_fight_red.id != null">
  <a class="a-white" href="/fights/{{fight.previous_fight_red.id}}">
    <div class="badge badge-info">{{fight.previous_fight_red.field_initial.name}}-{{fight.previous_fight_red.number}}</div>
  </a>
  </div>
  <span ng-show="fight.previous_fight_red_id == null">No previous red fight</span> 
</p>

<div class="form-actions">
  <a href="/fights/{{fight.id}}/edit"
    class="btn btn-primary"><i class="icon-edit"></i>
    Edit Fight
  </a>
  <button ng-click="destroy()" class="btn btn-danger">
    <i class="icon-trash"></i> Delete
  </button>
  <a href="/fights" class="btn">
    Back to fights
  </a>
</div>

